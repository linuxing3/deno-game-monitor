export interface Product {
    id: string;
    name: string;
    description: string;
    price: number;
}

export interface Game {
    id: string;
    name: string;
    description: string;
    pid: number;
    timestramp: string;
}
